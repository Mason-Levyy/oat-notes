## Summary

<!-- What changed and why, in a few lines. Link the issue if there is one. -->

## Test plan

- [ ] `uv run ruff check src tests` is clean
- [ ] `uv run pytest` passes
- [ ] `tests/test_local_only.py` still passes (nothing captured can leave the machine or reach a log)
- [ ] Manually verified: <!-- what you ran, e.g. `uv run oat-notes --ui` and started a meeting -->
